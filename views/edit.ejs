<%-include("partials/header")-%>

<div class="ui main text container segment">
	<div class="ui huge header">Edit <%= blog.title %></div>

	<form class="ui form"action="/blogs/<%= blog._id %>?_method=PUT" method="POST"><!--blog[] create a pseudo array called 'blog' that has a title, image, and body atttribute, ?_method=PUT because HTML doesnt support PUT methods so we're using a method override (workaround with express) OVERRIDES THE ORIGINAL METHOD INTO THIS ONE W UNDERSCORE-->
		<div class="field">
			<label>Title</label>
			<input type="text" name="title" placeholder="Title" value="<%= blog.title %>">
		</div>
		<div class="field">
			<label>Author</label>
			<input type="text" name="author" placeholder="Author URL" autocomplete="off">
		</div>
		<div class="field">
			<label>Image</label>
			<input type="text" name="image" placeholder="Image" value="<%= blog.image %>">
		</div>
		<div class="field">
			<label>Blog Content</label>
			<textarea name="body"><%= blog.body %></textarea>
		</div>
		
		<input class="ui violet basic button" type="submit" value="Publish">
	</form>
</div>
<%-include("partials/footer")-%>